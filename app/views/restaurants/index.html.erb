<div class="container">
  <% @restaurants.each do |restaurant| %>
    <h2><%= link_to "#{restaurant.name}", restaurant_path(restaurant.id), class: 'restaurant-name' %></h2>
  <% end %>
  <%= link_to '+ New Restaurant', new_restaurant_path %>
</div>
